<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">

  <item
      android:id="@+id/action_chatroom"
      android:title="@string/label_chatroom"
      android:icon="@drawable/ic_action_chat"
      app:showAsAction="ifRoom"/>

  <item
      android:id="@+id/action_tasks"
      android:title="@string/label_tasks"
      android:icon="@drawable/ic_tasks"
      app:showAsAction="ifRoom"/>

  <item
      android:id="@+id/action_team"
      android:title="@string/label_team"
      app:showAsAction="never"/>

</menu>